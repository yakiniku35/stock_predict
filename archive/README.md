# Archive

Legacy files kept for reference, not used by the current runtime path.

- `legacy_frontend_pipeline/`: older duplicate crawler and model scripts that lived under `frontend/`.
- `public_examples/`: old standalone demo/test pages. The supported UI is `public/dashboard.html`.

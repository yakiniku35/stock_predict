# Archive

Legacy files kept for reference, not used by the current runtime path.

- `legacy_frontend_pipeline/`: older duplicate crawler and model scripts that lived under `frontend/`.
- `public_examples/`: old standalone demo/test pages. The supported UI is `public/index.html`.

## legacy_public/

`public/` 舊版的靜態頁面，v2 已由 `public/index.html` + `styles.css` + `app.js` 取代，
保留在這裡僅供對照：

- `dashboard.html`：舊版儀表板頁面（使用已移除的 `model_forecasts` 欄位）。
- `recharts-stock-chart.jsx`：未被專案引用的 React 圖表片段。
